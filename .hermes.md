# simple-h1 (Quectel PI H1 / QCS6490) 简化 SDK

本项目是 Quectel PI H1 (QCS6490) 开发板的**简化 SDK**，脱离 Yocto/bitbake，仅两个功能：
1. **内核编译打包** → efi.bin (UKI)
2. **overlay 文件系统定制** → system.img

## ⚠️ 强制规则

### 第一步：读取 skill 文件

本项目操作指南在 `skills/` 目录下的 `*-SKILL.md` 文件中。**执行任何操作前必须先读取对应 skill**：

| 关键词 | skill 文件 |
|--------|-----------|
| 编译/内核/打包/efi.bin | `skills/simple-h1-build-SKILL.md` |
| 烧录/EDL/9008/qdl | `skills/simple-h1-flash-SKILL.md` |

### 第二步：Agent 工作流程

1. **修改代码/overlay**：直接修改，不要执行编译命令
2. **提示用户编译**：
   - 应用层/overlay 改动：`SKIP_KERNEL=1 ./scripts/build-all.sh`（快）
   - 内核改动：`./scripts/build-all.sh`（完整）
3. **用户确认打包完成后**：`./scripts/flash.sh` 烧录验证

### 重要警告

- **不要 kill/pkill bitbake 进程**（本 SDK 无 bitbake，但同样不要强杀编译进程）
- **不要执行 source/buildconfig/bitbake**（原生 SDK 命令，本工程不适用）
- **QCS6490 重启必须用 `adb shell reboot`**，`adb reboot` 不管用！
- **进 EDL 用 `adb shell reboot edl`**
- 内核调试原则：一次性加够 debug 信息，避免反复编译（内核编译 10-30 分钟）

## 目录速览

```
kernel/       内核源码 (6.6.116 QCOM 定制)
overlay/      ★ 文件系统增量目录 (打包合并进 system.img)
projects/     应用工程目录 (newapp 创建; make install 安装到 overlay)
prebuilds/    原始镜像 (system.img/efi.bin/dtb.bin/分区表)
tools/        adb/qdl/ukify/stub/initramfs
toolchains/   独立交叉编译链 (aarch64-qcom-linux-gcc 13.4.0)
scripts/      构建与烧录脚本
skills/       AI skills
docs/         文档
build/        编译产物 (build/result/ 为最终固件)
```

## 环境

```bash
source scripts/env.sh     # 原生入口
# 或推荐: source build.sh   (与 QPi-SDK/M2 命令兼容层, 自动 source env.sh)
```

## 与 QPi-SDK (M2) 命令兼容

本 SDK 提供 M2 同款命令层 (build.sh / Makefile / tools/build-*.sh), 同一套命令两 SDK 通用:
- `source build.sh` → buildenv/buildfetch/buildkernel/buildboot/buildrootfs/buildall/buildclean/newapp/buildapp 等
- `make check|prebuilds|kernel|boot|rootfs|all|clean|...`
- `./tools/build-kernel.sh <子命令>` / `./tools/build-rootfs.sh <子命令>`

映射: buildkernel→scripts/build-kernel.sh, buildboot→pack-efi.sh+pack-dtb.sh,
buildrootfs→tools/build-rootfs.sh build, buildall→(kernel+pack-efi+pack-dtb+build-rootfs),
buildenv→依赖安装+**底包下载**+sysroot+校验。

## 固件底包 (prebuilds/, 不入库)

底包数 GB, 不进仓库; 新 clone 的机器上 prebuilds/ 为空, 下载一条命令:
`./tools/fetch-prebuilds.sh fetch`  (= `buildfetch fetch` = `make prebuilds`)。
默认官方地址 https://developer.quectel.com/doc/files/quectel_pi/Quectel_Pi_H1_WF_Debian_RD2_Latest.zip
(指向厂商最新底包, 随厂商更新; ~3.2GiB, 断点续传, 缓存 download/)。

完整性以厂商 md5 为准 —— 工具链会主动取用同目录的 *_md5.txt:
  https://developer.quectel.com/doc/files/quectel_pi/Quectel_Pi_H1_WF_Debian_RD2_Latest_md5.txt

  * 打包 system.img 之前 (buildrootfs/buildall) 自动校验:
      联网失败            -> 用本地底包继续, 不阻塞构建
      md5 一致            -> 本地已最新, 继续打包
      md5 变了(厂商更新)  -> 下载最新包 -> 校验 -> 替换本地 -> 重新解压 -> 继续打包
      已更新但下载失败     -> 中止 (QPI_ALLOW_STALE=1 可强行用旧底包)
      开关: QPI_NO_REFRESH=1 跳过检查
  * fetch 时也校验: 本地 md5 不一致即重下; 下载完成后强制按厂商 md5 校验
  * 替换底包时旧原版不丢: system.img -> system.img.prev; 派生的 base_rootfs/sysroot
    移到 .prev 并重建

新产物不覆盖原版: 生成的 system.img 只写 build/result/system.img,
prebuilds/system.img 全程只读; 若两者被设成同一路径, build-rootfs.sh 直接报错拒绝。

本地 tools/prebuilds.sha256 只是复现记录(自我引用), 不能证明与厂商源一致;
md5 一致而 sha256 不符时会以厂商为准并自动刷新该记录。
子命令: check | fetch | refresh | md5 | verify | hash | pin | clean

buildenv 的 [2/4] 段会自动检查底包: 交互终端询问后下载; 非交互默认不下载 (避免数 GB 下载
无提示挂住扩展/CI), 需要时用 QPI_AUTO_FETCH=1; QPI_NO_FETCH=1 彻底禁用。
